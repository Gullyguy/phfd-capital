services:
  phfd-capital:
    build: .
    ports:
      - "8000:8000"
    environment:
      PHFD_ADMIN_USER: admin
      PHFD_ADMIN_PASSWORD: change-me-now
      LIVE_PROVIDER_MODE: "false"
      PHFD_DB_PATH: /app/data/phfd_capital.db
    volumes:
      - ./data:/app/data
